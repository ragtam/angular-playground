import { MenuForDirective } from './menu-for.directive';

describe('MenuForDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuForDirective();
    expect(directive).toBeTruthy();
  });
});
